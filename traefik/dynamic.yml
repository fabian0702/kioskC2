http:
  middlewares:
    oidc-auth:
      plugin:
        traefik-oidc-auth:
          Provider:
            Url: https://auth.localhost
            ClientId: traefik
            ClientSecret: traefik-local-secret
            InsecureSkipVerify: true
          LogLevel: DEBUG
          BypassAuthenticationRule: >
            PathPrefix(`/client`) ||
            Host(`auth.localhost`) || Host(`auth.kiosk.mnta.in`) ||
            HostRegexp(`[0-9a-f]{16}\..*`) || HostRegexp(`clients\..*`)
          Scopes:
            - openid
            - profile
            - email
          SessionCookie:
            Secure: false
